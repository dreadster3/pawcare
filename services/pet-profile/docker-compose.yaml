name: profile
services:
  server:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "8000:8080"
    environment:
      DB_NAME: petprofile
      DB_HOST: db
      DB_PORT: 27017
      DB_USER: user
      DB_PASSWORD: password

  db:
    image: mongo:latest
    ports:
      - "27017:27017"
    environment:
      MONGO_INITDB_ROOT_USERNAME: user
      MONGO_INITDB_ROOT_PASSWORD: password
      MONGO_INITDB_DATABASE: petprofile
    restart: always
